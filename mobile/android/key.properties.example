# Copy this file to key.properties (same folder) and fill in your keystore details.
# NEVER commit key.properties to git — it contains your signing credentials.

storePassword=YOUR_KEYSTORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=fyc
storeFile=../fyc-release.jks
