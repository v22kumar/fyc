# NOTE: keep this portable — no machine-specific absolute paths here.
# If you need a custom SSL trustStore (corporate network), put it in your
# USER-level gradle file instead: ~/.gradle/gradle.properties  (or
# %USERPROFILE%\.gradle\gradle.properties on Windows). Committing an absolute
# Windows path here breaks CI and any other machine's build.
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
