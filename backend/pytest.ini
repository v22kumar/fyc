[pytest]
# Only the real suite under tests/. The scripts/ directory holds manual
# integration/dev utilities (some named test_*) that talk to a live DB/network
# and are not part of CI — collecting them broke the run.
testpaths = tests
